# CS2 Meta-Analysis Engine — Python dependencies

# Demo parsing (Rust-based, fastest available)
demoparser2

# Spatial / CS2 framework
awpy>=2.0.0

# Data science
pandas>=2.0.0
numpy>=1.24.0
scikit-learn>=1.3.0

# Web framework & async
fastapi>=0.110.0
uvicorn[standard]>=0.29.0
python-multipart>=0.0.9

# RCON
aiorcon>=0.2.0

# HTTP / scraping for HLTV (curl_cffi bypasses Cloudflare TLS fingerprinting)
requests>=2.31.0
curl_cffi>=0.7.0
beautifulsoup4>=4.12.0
lxml>=5.1.0
rarfile>=4.2
tqdm>=4.66.0
zstandard>=0.22.0

# AI (match recap)
anthropic>=0.40.0

# Database (PostgreSQL / Supabase — optional, falls back to SQLite)
psycopg2-binary>=2.9.9

# Utilities
pydantic>=2.0.0
pydantic-settings>=2.0.0
python-dotenv>=1.0.0
aiofiles>=23.2.0
httpx>=0.27.0
